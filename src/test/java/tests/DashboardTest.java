/*
package tests;

import common.BaseTest;
import io.qameta.allure.*;
import org.testng.Assert;
import org.testng.annotations.Test;

@Epic("DashboardFeature")
public class DashboardTest extends BaseTest {
    @Test(description = "Verify Dashboard displayed")
    @Severity(SeverityLevel.CRITICAL)
    @Description("Test Description: Login successful & Dashboard displayed")
    @Story("Dashboard isDisplayed")
    public void Dashboard_isDisplayed(){
        loginToApplication();
        this.dashboard.getDashboardTitle();
        String actualTitle = this.dashboard.getDashboardTitle();
        String expectTitle = "HỆ THỐNG ĐỐI SOÁT";
        Assert.assertEquals(actualTitle,expectTitle,"Dash board hiển thị không đúng");
    }
    @Test(description = "Verify filter data into Bản kê chi tiết đối soát KH thường")
    @Severity(SeverityLevel.CRITICAL)
    @Story("Filter Data BKCTDSKHT")
    public void Verify_FilterData_BKCTDSKHT(){
        loginToApplication();
        this.dashboard.filter_KHT();
    }
}
*/
